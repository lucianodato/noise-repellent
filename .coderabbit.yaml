# CodeRabbit Configuration File
language: "en-US"
reviews:
  profile: "chill"
  auto_review:
    enabled: false
    drafts: false
  auto_approve: true
  path_instructions:
    - path: "Source/**/*"
      instructions: |
        Pay attention to JUCE real-time audio safety and parameter architecture:
        - Strict real-time safety compliance in processBlock() (no allocations, no locks, no I/O).
        - Ensure APVTS parameter IDs match layout definitions in PluginProcessor.cpp.
        - Verify juce::dsp::DryWetMixer latency alignment and thread-safe lock-free SPSC FIFO operations.
chat:
  auto_reply: true
